<Window x:Class="CbtExam.Desktop.Views.BroadcastDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Broadcast Message" Height="340" Width="480"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Segoe UI">

    <Border Background="{DynamicResource BgBrush}" CornerRadius="20" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="40" Color="Black" Opacity="0.15" ShadowDepth="10"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource AccentBrush}" CornerRadius="20,20,0,0" Padding="28,20">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Broadcast Message" FontSize="20" FontWeight="Black" Foreground="White"/>
                        <TextBlock Text="Send an alert or instruction to all candidates in the waiting room." FontSize="13" Foreground="White" Opacity="0.8" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Width="28" Height="28" Background="#33FFFFFF" BorderThickness="0" 
                            HorizontalAlignment="Right" VerticalAlignment="Top" Click="CancelButton_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="14">
                                    <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10" 
                                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Content Area -->
            <StackPanel Grid.Row="1" Margin="28,20,28,0">
                <TextBlock Text="MESSAGE TEXT" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="2,0,0,8"/>
                <Border Background="{DynamicResource InputBgBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1.5" CornerRadius="12" Padding="12">
                    <TextBox x:Name="MessageTextBox" Height="100" BorderThickness="0" Background="Transparent" 
                             GotFocus="MessageTextBox_GotFocus"
                             Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                </Border>
            </StackPanel>

            <!-- Actions -->
            <Border Grid.Row="2" Padding="28,0,28,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="1" Content="Cancel" Style="{StaticResource PrimaryBtn}" 
                            Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource TextSecondaryBrush}" 
                            Padding="20,10" Margin="0,0,12,0" Click="CancelButton_Click"/>
                            
                    <Button Grid.Column="2" Content="Broadcast" Style="{StaticResource PrimaryBtn}" 
                            Background="{DynamicResource AccentBrush}" Foreground="White" Padding="24,10" Click="SendButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
